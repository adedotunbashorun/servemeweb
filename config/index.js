
export const config = {
         apiUrl:
           process.env.NODE_ENV === "production"
             ? "https://servemeserviceappapi.herokuapp.com"
             : "https://servemeserviceappapi.herokuapp.com"
       };
